# Anthropic API key — required for AI goal decomposition
ANTHROPIC_API_KEY=sk-ant-...

# SQLite DB path (optional — defaults to ../skills.db)
# DB_PATH=/custom/path/skills.db

# Server port (optional — defaults to 8000)
# PORT=8000
